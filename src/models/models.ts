export interface GlobalData {
  rewardPool: number;
  equityPool: number;
  totalShares: number;
}

export interface StakeColumnData {
  id: string;
  fenix: number;
  term: number;
  timeBonus: number;
  sizeBonus: number;
  totalBonus: number;
  shares: number;
}
